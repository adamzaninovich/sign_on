= link_to "Sign On", root_path, :class => 'brand'
%ul.nav.pull-right
  - if current_user
    %li= link_to current_user.name || current_user.email, account_path
    %li= link_to "Sign out", signout_path
  - else
    %li= link_to "Sign up", signup_path
    %li= link_to "Sign in", signin_path
